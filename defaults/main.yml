---
# defaults file for docker-gitlab

# The gitlab Community Edition release.
docker_gitlab_version: 11.1.4            # https://about.gitlab.com/blog/

# The default directory to store the docker data.
docker_gitlab_root_data_dir: /home/{{ ansible_user_id }}/gitlab_docker_data
